LASTFM_API_KEY=your_api_key_here
# Override the SQLite database path (defaults to timetraveler.db in the working directory).
# In production the database is stored on an Azure Files volume mounted at /data.
DB_PATH=/data/timetraveler.db
